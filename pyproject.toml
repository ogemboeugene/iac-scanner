[build-system]
requires = ["setuptools>=82.0.1", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "iac-scanner"
# Version is derived from the latest signed git tag (vX.Y.Z) by setuptools-scm.
# Do NOT set `version =` here. See [tool.setuptools_scm] below and CONTRIBUTING.md → "Releases".
dynamic = ["version"]
description = "AI-assisted IaC scanner and autofix for Terraform and AWS CDK. Complements rule-based scanners with LLM-generated fixes."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [
    { name = "Bishwas Jha", email = "jha.bishwas@gmail.com" }
]
keywords = [
    "terraform",
    "aws-cdk",
    "iac",
    "infrastructure-as-code",
    "security",
    "sast",
    "sarif",
    "devsecops",
    "ai",
    "langchain",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: Information Technology",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Security",
    "Topic :: Software Development :: Quality Assurance",
    "Typing :: Typed",
]
dependencies = [
    "click>=8.3.3",
    "langchain>=0.3.0,<0.4.0",
    "langchain-openai>=0.2.0,<0.4.0",
    "langchain-anthropic>=0.2.0,<0.4.0",
    "langchain-core>=0.3.0,<0.4.0",
    "langchain-community>=0.3.0,<0.5.0",
    "pydantic>=2.13.4,<3.0.0",
    "tiktoken>=0.7.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=4.0.0",
    "respx>=0.23.1",
    "ruff>=0.4.0",
    "mypy>=1.10.0",
    "bandit[toml]>=1.9.4",
    "pip-audit>=2.10.0",
    "cyclonedx-bom>=4.0.0",
    "build>=1.5.0",
    "pre-commit>=3.0.0",
]
# Local LLM via Ollama (no API key required)
local = [
    "langchain-ollama>=0.2.0",
]
# GitHub Models provider — free for GitHub-authenticated users.
# Uses langchain-openai with a custom base_url + GITHUB_TOKEN as the API key.
# No additional runtime deps beyond the base langchain-openai.
github = []
# MCP server mode — expose iac-scanner as tools to Claude Desktop / Cursor / Continue.dev.
# Host app supplies the LLM; iac-scanner does not need any LLM API key.
mcp = [
    "mcp>=1.27.1",
]
# Rule-engine hybrid mode — Checkov pre-pass + LLM augment + post-fix verification.
rules = [
    "checkov>=3.0.0,<4.0.0",
]
# Everything.
all = [
    "langchain-ollama>=0.2.0",
    "mcp>=1.27.1",
    "checkov>=3.0.0,<4.0.0",
]

[project.scripts]
iac-scan = "iac_scanner.cli:main"
iac-scan-mcp = "iac_scanner.mcp_server:main"

[project.urls]
Homepage = "https://github.com/bishwasjha/iac-scanner"
Repository = "https://github.com/bishwasjha/iac-scanner"
Documentation = "https://bishwasjha.github.io/iac-scanner/"
Issues = "https://github.com/bishwasjha/iac-scanner/issues"
Changelog = "https://github.com/bishwasjha/iac-scanner/blob/main/CHANGELOG.md"

[tool.setuptools.packages.find]
where = ["src"]

# Ship the PEP 561 marker so downstream packages (and mypy running in other
# repos) recognize this package as typed.
[tool.setuptools.package-data]
iac_scanner = ["py.typed"]

# ---------------------------------------------------------------------------
# Versioning: setuptools-scm derives the package version from git tags.
#
#   git tag -s v0.5.0 -m "Release 0.5.0"   →   iac-scanner==0.5.0
#   (no tag, dirty tree)                   →   iac-scanner==0.5.1.devN+gSHA[.dYYYYMMDD]
#
# `version_file` writes a tracked-but-gitignored module so editable installs
# and `iac_scanner.__version__` work without re-running `pip install -e .`.
# ---------------------------------------------------------------------------
[tool.setuptools_scm]
version_file = "src/iac_scanner/_version.py"
# Accept tags like "v1.2.3" and "v1.2.3rc1".
tag_regex = "^v(?P<version>[0-9]+\\.[0-9]+\\.[0-9]+(?:[._-]?(?:a|b|c|rc|alpha|beta|pre|preview)\\d*)?(?:\\.post\\d+)?(?:\\.dev\\d+)?)$"
# PEP 440-compatible dev version between tags.
version_scheme = "guess-next-dev"
local_scheme = "node-and-date"
# Fallback used in source trees without git metadata (e.g. unpacked sdist with no PKG-INFO).
fallback_version = "0.0.0+unknown"

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-ra --strict-markers --strict-config"
testpaths = ["tests"]
markers = [
    "e2e: end-to-end tests that hit real LLM APIs (opt-in, nightly only)",
    "smoke: quick smoke tests that run in CI without external services",
    "integration: integration tests with mocked LLM providers",
]

[tool.coverage.run]
source = ["src/iac_scanner"]
branch = true
omit = [
    "src/iac_scanner/mcp_server.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "raise NotImplementedError",
    "if TYPE_CHECKING:",
    "if __name__ == .__main__.:",
]
show_missing = true
skip_covered = false

[tool.mypy]
# Bumped to 3.12 to match the CI runtime and let mypy parse numpy>=2.4's
# stubs (which use PEP 695 `type X = ...` syntax, only valid on 3.12+).
# Runtime code stays 3.10-compatible — pytest still covers 3.10/3.11/3.12/3.13.
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true

[[tool.mypy.overrides]]
module = [
    "checkov.*",
    "langchain_ollama.*",
    "mcp.*",
]
ignore_missing_imports = true

# numpy>=2.4 ships .pyi stubs that use PEP 695 `type X = ...` syntax; mypy
# targeting python_version = "3.10" cannot parse it. numpy is a transitive
# dep of langchain-community + checkov; we do not import it directly. Skip
# following the imports so the syntax error can't reach us.
[[tool.mypy.overrides]]
module = ["numpy.*"]
follow_imports = "skip"
ignore_errors = true

[tool.bandit]
exclude_dirs = ["tests", "samples", "docs"]
# B101 — assert_used: pytest uses assert.
# B310 — urllib.urlopen: only called in providers.py with an http(s) scheme guard upstream.
skips = ["B101", "B310"]
